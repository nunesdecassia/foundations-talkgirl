# GirlTalk - CODE Foundations

Descricao

## Quickstart

Install `pipenv`:

> pip install pipenv

Activate the virtual environment:

> pipenv shell

Install all dependencies:

> pipenv install

Start the project:

> python run_dev.py
